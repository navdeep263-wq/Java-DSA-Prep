class stringMethods{
    public static void main(String[] args) {
        // String s = "programming";

        // //substring() method
        // System.out.println(s.substring(3));

        // //syntax 2 of substring
        // System.out.println(s.substring(3, 7));

        // //indexOf() method
        // System.out.println(s.indexOf('g'));

        // //example
        // System.out.println(s.substring(s.indexOf('m')));

        //practice
        //find how many g are in this string
        // int count = 0;
        // char target = 'm';
        // for(int i = 0; i <= s.length() - 1; i++){
        //      if(s.charAt(i) == target){
        //            count++;
        //      }
        // }
        // System.out.println("count of m is :" + count);

        //find the number of vowels
            // int count = 0;
            // for(int i = 0; i < s.length(); i++){
            //     if(s.charAt(i) == 'a' || s.charAt(i) == 'e' || s.charAt(i) == 'i' || s.charAt(i) == 'o' || s.charAt(i) == 'u'){
            //         count++;
            //     }
            // }
            // System.out.println("vowels are : " + count);

            //first letter upper to convert lower or check vowel or not

            // String s = "Apple";
            // System.out.println(s.toLowerCase());
            
            // int count = 0;
            // for(int i = 0; i < s.length(); i++){
            //     if(s.charAt(i) == 'a' || s.charAt(i) == 'e' || s.charAt(i) == 'i' || s.charAt(i) == 'o' || s.charAt(i) == 'u' || s.charAt(i) == 'A' ||s.charAt(i) == 'E' || s.charAt(i) == 'I' || s.charAt(i) == 'O' || s.charAt(i) == 'U'){
            //         count++;
            //     }
            // }
            // System.out.println("vowels are : " + count);

            
            
        }
    }
    
